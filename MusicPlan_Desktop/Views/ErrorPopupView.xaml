<UserControl x:Class="MusicPlan_Desktop.Views.ErrorPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:resources="clr-namespace:MusicPlan_Desktop.Resources"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Label FontSize="18" 
                   ContentStringFormat="{x:Static resources:ApplicationResources.ErrorStringFormat}" 
                   Content="{Binding Content}" Foreground="Red" Padding="20"/>
        </StackPanel>

    </Grid>
</UserControl>
